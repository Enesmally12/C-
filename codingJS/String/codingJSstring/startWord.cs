// namespace cSharp_withMrMike.String;

// // Given a string and a second "word" string, we'll say that the word matches the string if it appears at the front of 
// // the string, except its first char does not need to match exactly. On a match, return the front of the string,
// //  or otherwise return the empty string. So, so with the string "hippo" the word "hi" returns "hi" and "xip" returns "hip".
// //  The word will be at least length 1.

// // Examples

// // startWord('hippo', 'hi') → hi
// // startWord('hippo', 'xip') → hip
// // startWord('hippo', 'i') → h

// public class StartWord
// {
//     public static string startword(string word1, string word2)
//     {
//         string newWord = "";
//         if(word1[0] == word2[0])
//         {
//             newWord = word2;
//         }
//         else
//         {
//             newWord += word1[0];
//             for( )
//         }


//         return newWord;
//     }
// }