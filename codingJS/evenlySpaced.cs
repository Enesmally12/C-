// namespace cSharp_withMrMike.evenlySpace;
// // Given three ints, a b c, one of them is small, one is medium and one is large. 
// // Return true if the three values are evenly spaced, so the difference between 
// // small and medium is the same as the difference between medium and large.

// // Examples

// // evenlySpaced(2, 4, 6) → true
// // evenlySpaced(4, 6, 2) → true
// // evenlySpaced(4, 6, 3) → false

// public class EvenlySpaced
// {
//     public void evenly(int a, int b, int c)
//     {
//         bool isResult;
//         if(Math.Abs( a-b) == 2 && Math.Abs(b-c) == 2 && Math.Abs(a-c) == 2)
//         {
//             isResult = true;
//             Console.WriteLine($"{isResult}");
//         }
//         else
//         {
//             isResult = false;
//             Console.WriteLine($"{isResult}");
//         }

//     }
    
// }
